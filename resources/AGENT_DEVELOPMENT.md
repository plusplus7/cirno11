# Agent Development Notes

- Start from `AGENTS.md`, then load only the resource file needed for the current task.
- Use OpenSpec changes for product or behavior changes that need proposal/spec/task tracking.
- Keep delivery notes in the relevant OpenSpec task or archive when a change is completed.
- Before handoff, run the automated checks listed in `resources/QA.md` when dependencies are available.
- Do not commit local secrets, generated release directories, uploaded media, or `dist/`.
